帮我设计一个黑板报网页，网页整体呈纵向布局，html和css为两个独立文件
## 整体结构
--黑板报整体，W=1440，H为动态变化
    --标题部分
    --表扬通报部分
    --违规通报部分

- 标题部分距离黑板报上部距离为200，距离左边为60
- 表扬通报部分距离标题部分距离为100，距离作边为30
- 违规通报部分距离表扬通报部分距离为100，距离作边为30

## 标题部分

标题部分是一个独立的整体，内容如下：

- 标题文字，两行，“信息技术部
黑板报”，字号150，颜色为白色，靠左对齐，文字间距10，行距20
- “黑板报”文字右侧100像素是今天的日期，字号为60，白色，对齐“黑板报”文字的右边，距离40
- 子标题“只要读懂了客户，科技的突破则水到渠成”，字号60，颜色为蓝色。子标题有一个矩形白色背景，靠左对齐，上边距离标题40
- 多行介绍文字“阳光科技人必须以“争创第一，追求极致”的心气和决心；
以最懂客户的底气；以最懂技术的能力；
以永不满足的进取精神；
渐进积累、颠覆突破、实现第一、靠近极致。
以科技创造未来！”，靠左对齐，上边距离子标题40


## 表扬通报部分

- 表扬通报部分是一个独立的整体，使用res/bg-commend.png为背景图。通报部分具体内容是一个表格，表格是表扬通报内的一个整体，两边对齐，距离为50，上边距离为100（表格的左右和上距离都是相对于父节点，也就是表扬通报部分整体）
- 表格表头有5个字段，分别是“姓名、所属处室、事件类型、表扬事件、累计”，表头背景为淡蓝色，字体加粗，字号为32，
- 表扬通报表格内容，为多行背景为白色

## 违规通报部分

- 违规通报部分同表扬通报部分，只是使用res/bg-notice.png为背景，表头背景为红色，表头字段为“姓名、所属处室、事件类型、违规事件、通报依据”

## 表扬数据来源
表扬数据来源于上传的excel的“【表扬】通报登记清单-表扬”页签，数据筛选规则如下：
- “事件发生时间”相对于黑板报设置的“日期”前7天的数据
- “状态”为非“（驳回）取消发布”，非“已发布”
- “处长/员工”为“处长”

筛选出的数据记录，和表扬通报表的字段对应关系如下：

- 姓名 - 姓名
- 表扬发起部门 - 所属处室
- 通报事件类型 - 事件类型
- 表扬内容...（以表扬内容开头的字段） - 表扬事件

“累计”字段统计规则如下：
- 统计表格中所有的、状态为非“（驳回）取消发布”状态，“处长/员工”为“员工”的数据
-